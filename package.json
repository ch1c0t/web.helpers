{
  "name": "web.helpers",
  "main": "lib/main.js",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "start": "coffee --watch --compile --output lib src",
    "spec": "jasmine"
  },
  "devDependencies": {
    "coffeescript": "^2.5.1",
    "jasmine": "^3.9.0",
    "playwright": "^1.14.1",
    "serve-handler": "^6.1.3"
  }
}
